/**
 * Player/Team statistics derived from match results.
 * All values are computed, never manually edited.
 */
export interface Stats {
  played: number;
  wins: number;
  draws: number;
  losses: number;
  points: number;
}
